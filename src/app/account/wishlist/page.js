import WishlistView from "@/components/account/WishlistView";

export const metadata = {
  title: "Wishlist",
  description: "The paddles, shoes and kit you have saved for later.",
};

export default function WishlistPage() {
  return <WishlistView />;
}
